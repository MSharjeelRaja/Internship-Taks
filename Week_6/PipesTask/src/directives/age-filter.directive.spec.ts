import { AgeFilterDirective } from './age-filter.directive';

describe('AgeFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeFilterDirective();
    expect(directive).toBeTruthy();
  });
});
