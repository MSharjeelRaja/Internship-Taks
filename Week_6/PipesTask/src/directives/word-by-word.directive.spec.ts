import { WordByWordDirective } from './word-by-word.directive';

describe('WordByWordDirective', () => {
  it('should create an instance', () => {
    const directive = new WordByWordDirective();
    expect(directive).toBeTruthy();
  });
});
