import { SpaceCheckerDirective } from './space-checker.directive';

describe('SpaceCheckerDirective', () => {
  it('should create an instance', () => {
    const directive = new SpaceCheckerDirective();
    expect(directive).toBeTruthy();
  });
});
